# Kainnne 網站偏好

## 文字

- 不使用品牌口號、情緒文案、抽象敘事或自我感動式描述。
- 不使用 `building now`、`made with curiosity`、`wander`、`beautifully useful` 等氣氛文字。
- 不用段落解釋訪客正在看的內容。
- 每段可見文字必須直接提供辨識、功能、狀態、導覽或操作資訊。
- 可以用短標籤、產品類型、技術名稱、功能清單與明確按鈕。
- 能刪除的文字直接刪除，不用另一句行銷文案替換。

## 專案卡片

- 第一眼必須看出產品類型與用途。
- 第一行使用具體分類，例如 `Markdown Desktop Reader`，不用抽象定位。
- 描述使用一個直接功能句。
- 補充資訊使用可掃讀的功能清單，不寫故事。

## 視覺與互動

- 以 WikiNB 的粉紅、薰衣草、淡藍與薄荷漸層作為主要視覺參考。
- 入口頁頂部導覽優先保留 `Projects`、`Contact`，不放左上角 Logo；其他位置若出現 `Kainnne` 字標，使用 WikiNB 同系圓角字體與彩色漸層。
- 互動應分布在背景、文字、連結、按鈕、篩選器和卡片，不增加無功能的獨立互動裝飾物。
- 動畫必須提供狀態、操作回饋或視覺層級；沒有功能的圖案直接刪除。
- 個人演奏與音樂會照片可作為頁面動態邊界：非手機畫面使用左右垂直反向循環相片軌，手機才改為頂部橫向半透明循環軌；照片保持足夠辨識度並使用輕微削角增加立體感，不做一般相簿輪播。

## 入口與導覽

- 首頁主要領域包含 `Apps Design`、`UI / UX`、`AI`、`Music`、`Machine Learning`，小標籤也要有微互動。
- 觸控裝置按住互動元件時，必須顯示與滑鼠指向一致的視覺回饋，不只做縮小效果。
- 首頁主要入口保留 `GitHub`、`Instagram`、`YT Music`；`Projects` 由頂部導覽與數字索引負責。`YT Music` 使用獨立色彩作為音樂入口。
- 專案少時不顯示分類篩選或搜尋，直接呈現專案卡片；每張卡片清楚分開 `Open` 正式網站與 `GitHub` 原始碼入口。
- 已有正式子網域的專案卡片直接開啟產品網站，不導向 GitHub repository。
- 保留 `01 / 02 / 03` 向下索引列，方便未來增加專案。
- 不使用大型 Contact 區塊；`Contact` 導覽以浮動面板顯示 Gmail 與 Instagram。
- 浮動面板開啟後，點擊面板外任意位置必須立即收回；`Projects`、`Contact` 在頂部框內置中且字級一致。
- `Projects` 頂部入口使用下拉面板直接列出 `01 / 02 / 03` 並跳到對應卡片；手機上的 `Projects`、`KCIS`、`Contact` 面板都必須在畫面中央完整顯示，不得因按鈕位置而被裁切。
- 機構型入口（例如 KCIS）放在頂部導覽的獨立浮動面板，不混入個人作品的數字索引；面板只放明確名稱、網址與可執行入口，並保留日後加入問卷等功能的空間。
- 寬螢幕頂部導覽的尺寸與 `01 / 02 / 03` 專案索引對齊，不使用過小的動態島比例；`KCIS` 保留既有按鈕與互動，只將四個字改為高飽和彩色字體。避免淡黃、粉白等會融入藍紫背景的低對比色，尤其 `K` 必須從清楚的高飽和色開始。
- 四個主要入口、`01 / 02 / 03` 索引與專案卡片要形成緊密連續的導覽，不用滿版高度或大片留白隔開。
- 專案卡片下方使用本機 `.md` 檔驅動可收合的 Q&A；公開頁面只有閱讀與展開功能，不顯示搜尋、編輯或 Codex 控制。
- Q&A 使用「總入口 → 分類 → 問題」三層收合結構；Markdown 只保留具有辨識價值的個人、研究、開發、軍事與音樂資訊，刪除重複問答。
- English／中文切換放在頂部亮暗模式旁並預設 English，不放進 Q&A 框內；同一開關控制專案卡片說明與 Q&A。翻譯皆由本機檔案手工維護，不使用自動翻譯，兩個版本必須維持相同結構與內容意義。
- 首頁大型 `Kainnne.` 字標直接導向 Q&A。
- 桌面頁尾的 `Kainnne` 靠分隔線左側對齊，四個導覽連結維持整體置中。
- 保留 Person／ProfilePage／WebSite 結構化資料、`robots.txt`、`sitemap.xml` 與頁尾免 Cookie 公開瀏覽數，作為網站目前的 SEO／GEO 基準。

## 判斷原則

1. 這段文字是否幫助辨識產品？
2. 是否說明具體功能、狀態或下一個操作？
3. 刪掉後是否完全不影響理解？若是，刪除。
