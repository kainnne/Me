{
  "name": "kainnne-portal",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "seo:generate": "node scripts/generate-seo.mjs",
    "seo:check": "node scripts/generate-seo.mjs --check",
    "build": "npm run resume:generate && npm run cake:generate && npm run seo:generate && tsc -b && vite build",
    "preview": "vite preview",
    "resume:generate": "node scripts/build-resume.mjs",
    "resume:check": "node scripts/build-resume.mjs --check",
    "cake:generate": "node scripts/build-cake.mjs",
    "cake:check": "node scripts/build-cake.mjs --check"
  },
  "dependencies": {
    "@paper-design/shaders-react": "^0.0.72",
    "lucide-react": "latest",
    "marked": "^15.0.12",
    "motion": "^13.0.0",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
